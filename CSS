<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="C:\Users\Lara_\OneDrive\Escritorio\ProyectoSGA\Css\men.css">
    <title>Sistema de Inventario</title>
</head>
<body>
    <header>
        <div class="logo">
            <img src="logo.img" alt="Logo de la empresa">  <!-- Aqui se agrega el logo del proyecto -->
        </div>
        <div class="Correo_electronico">
            <i class="fas fa-user"></i> <!-- se usa para el encabezado de la pagina  -->
            <span>Bryant@correo.com</span> 
        </div>
    </header>
    <div class="contenedor">
    
        <nav class="lista">    <!-- Se usa para crear un menu lateral -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="C:\Users\Lara_\OneDrive\Escritorio\ProyectoSGA\Css\men.css">
    <title>Sistema de Inventario</title>
</head>
<body>
    <header>
        <div class="logo">
            <img src="logo.img" alt="Logo de la empresa">  <!-- Aqui se agrega el logo del proyecto -->
        </div>
        <div class="Correo_electronico">
            <i class="fas fa-user"></i> <!-- se usa para el encabezado de la pagina  -->
            <span>Bryant@correo.com</span> 
        </div>
    </header>
    <div class="contenedor">
    
        <nav class="lista">    <!-- Se usa para crear un menu lateral -->
      …
[7:45 p. m., 5/9/2023] +57 319 6796265: body {
    font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    margin: 0;
    padding: 0;
}

header {
    background: linear-gradient(#A569BD,#82E0AA );  
    color: hsl(0, 0%, 6%);
    padding: 20px;
  
    display: flex;
    justify-content: space-between; /* genera un espacio entre elementos */
   
}

.logo{
    text-align: left;
    padding-left: 10px;
}

.logo img {
    max-width: 100%;
    height: auto;  /* esto hace que la imagen se ajuste dentro dentro de un elemento */
}

.Correo_electronico{

    margin-right: 10px;
}



.contenedor{
    display: flex;
    height: 580px;  /* ocupa el 100% de alto de la pagina  */
    
}

/* stilos menu lateral */
.lista {
    background: linear-gradient(#A569BD,#82E0AA );  
    color: #fff;
    width: 250px;
    padding: 30px;
}

.lista ul {
    list-style: none; /* para quitar viñetas del menu lateral   */
    padding: 0;
}

.lista ul li {
    margin-bottom: 20px; /* espacio entre listas */
} 

.lista ul li a {
    text-decoration: none; /* quita elementos subrayados  */
    color: #1a1919;
    font-weight: bold;
    padding: 5px;
}

/* Estilos del contenido */
.cajitas {
    flex-grow: 1;
    padding: 0.5cm;
}

.row {
    display: flex  ;
    height: 200px;
    padding: 20px;
  
}

/* stilos categorias */
.cajitas2{
    
    display: flex;
    height: 200px;
    background-color:#fcfcfc;
    width: 300px;
    border: black 1px solid; 
    margin: 5px;
    border-radius: 10px;
}

.cajitas h2 {
    margin: 0;
}
